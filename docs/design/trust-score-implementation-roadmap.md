# 信頼性スコアシステム実装ロードマップ

**作成日**: 2025-11-14
**ステータス**: 実装中
**目的**: エージェントハブの信頼性スコアベース評価システムの優先度順実装計画

---

## 📋 実装の全体方針

### 基本原則
1. **段階的実装**: コア機能から順に実装し、各フェーズで動作確認
2. **データ駆動**: スコアと判定根拠を常にDBに記録し、透明性を確保
3. **自動化優先**: 人間の介入は例外ケースのみ
4. **安全性重視**: 自動承認は厳しい基準、自動リジェクトは早めに判断

### 実装完了状態（2025-11-14時点）

#### ✅ Phase 1: データ基盤とスコア算出
- [x] DBスキーマ拡張（`submissions`テーブルにスコアカラム追加）
- [x] `trust_score_history`テーブル作成（監査ログ）
- [x] Temporal Workflowへのスコア算出関数追加
  - `calculateSecurityScore()`: Security Gate結果から0-30点算出
  - `calculateFunctionalScore()`: Functional Accuracy結果から0-40点算出
  - `calculateJudgeScore()`: Judge Panel結果から0-20点算出
  - `calculateImplementationScore()`: 固定10点（将来的に実装品質評価）
  - `calculateTrustScore()`: 総合スコアと自動判定決定
- [x] スコア算出後のイベント発行（`trust_score_calculated`）
- [x] LLM Judge Panel アンサンブル設計書作成

---

## 🎯 優先度順実装タスク

### Phase 1.5: 自動判定分岐ロジック（最優先 🔴）

**目的**: 算出した信頼性スコアに基づき、ワークフローが自動的に承認/リジェクト/人間レビューに分岐する

**タスク**:
1. **Workflow分岐実装** (`reviewPipeline.workflow.ts`)
   ```typescript
   // Judge Panel実行後、trustScoreを算出済み
   if (trustScore.autoDecision === 'auto_rejected') {
     // 自動リジェクト: submissionStateをrejectedに更新
     await activities.updateSubmissionState({
       submissionId,
       state: 'rejected',
       trustScore: trustScore.total,
       scoreBreakdown: JSON.stringify(trustScore),
       autoDecision: 'auto_rejected'
     });
     return { outcome: 'auto_rejected', trustScore };
   } else if (trustScore.autoDecision === 'auto_approved') {
     // 自動承認: submissionStateをapprovedに更新
     await activities.updateSubmissionState({
       submissionId,
       state: 'approved',
       trustScore: trustScore.total,
       scoreBreakdown: JSON.stringify(trustScore),
       autoDecision: 'auto_approved'
     });
     return { outcome: 'auto_approved', trustScore };
   } else {
     // 人間レビュー必要: 既存のHuman Reviewフローへ
     currentStage = 'human_review';
     // ... 既存のHuman Reviewロジック
   }
   ```

2. **Activity実装** (`activities/updateSubmissionState.ts`)
   - `submissions`テーブルへのスコア永続化
   - `trust_score_history`への履歴記録
   - `state`, `trust_score`, `score_breakdown`, `auto_decision`の更新

3. **テスト**:
   - スコア<40のケース（自動リジェクト）
   - スコア≥80のケース（自動承認）
   - スコア40-79のケース（人間レビュー）

**成果物**:
- Workflow分岐ロジック実装
- DB更新Activity実装
- 自動判定の動作確認

---

### Phase 2: Review UI スコアカード表示（優先度: 高 🟠）

**目的**: レビュー管理者が信頼性スコアの内訳と判定根拠を確認できる

**タスク**:
1. **スコアカードコンポーネント作成** (`review-ui/src/components/TrustScoreCard.tsx`)
   ```tsx
   interface TrustScoreCardProps {
     trustScore: number;
     breakdown: {
       security: number;
       functional: number;
       judge: number;
       implementation: number;
     };
     reasoning: {
       security?: string;
       functional?: string;
       judge?: string;
       implementation?: string;
     };
     autoDecision: 'auto_approved' | 'auto_rejected' | 'requires_human_review';
   }
   ```

2. **スコア表示UIデザイン**:
   - 総合スコア（0-100）の大きな表示
   - 各ステージのスコア内訳（プログレスバー）
   - 自動判定結果のバッジ（色分け）
   - 各ステージの判定根拠（展開可能）

3. **Submission詳細ページへの統合** (`review-ui/src/app/submissions/[id]/page.tsx`)
   - API経由でスコア情報取得
   - スコアカードをステージイベントの上部に表示

**成果物**:
- TrustScoreCardコンポーネント
- Submission詳細ページへの統合
- スコア可視化UI

---

### Phase 3: Submission UI（エージェント登録画面）（優先度: 高 🟠）

**目的**: 企業ユーザーがWebブラウザからエージェントを登録できる

**タスク**:
1. **新規Next.jsアプリ作成** (`submission-ui/`)
   - Next.js 14 App Router
   - TypeScript + Tailwind CSS
   - shadcn/uiコンポーネント

2. **エージェント登録フォーム** (`submission-ui/src/app/register/page.tsx`)
   - エージェントカードURL入力
   - エンドポイントURL入力（自動検出可能）
   - 署名バンドルのアップロード（オプション）
   - 組織メタデータ入力（組織名、連絡先）
   - バリデーション（URLフォーマット、到達性確認）

3. **API連携**:
   - POST `/api/submissions` への送信
   - 登録後のSubmission ID取得
   - ステータス確認ページへリダイレクト

4. **登録状況確認ページ** (`submission-ui/src/app/status/[id]/page.tsx`)
   - リアルタイムステータス表示（Temporal Query）
   - 各ステージの進行状況
   - スコア表示（算出後）
   - 最終判定結果

**成果物**:
- Submission UI (Next.js app)
- エージェント登録フォーム
- ステータス確認ページ

---

### Phase 4: JWT認証実装（優先度: 中 🟡）

**目的**: 企業ユーザーとレビュー管理者の認証・認可

**タスク**:
1. **認証サーバー構築** (`auth-service/`)
   - Express.js + TypeScript
   - JWT発行・検証
   - パスワードハッシュ化（bcrypt）
   - リフレッシュトークン

2. **ユーザーDBスキーマ** (`db/migrations/20251114_auth.sql`)
   ```sql
   CREATE TABLE users (
     id UUID PRIMARY KEY,
     email TEXT UNIQUE NOT NULL,
     password_hash TEXT NOT NULL,
     role TEXT CHECK (role IN ('company', 'reviewer', 'admin')),
     organization_id UUID,
     created_at TIMESTAMP WITH TIME ZONE DEFAULT now()
   );
   ```

3. **API Gateway認証ミドルウェア** (`api/src/middleware/auth.ts`)
   - JWT検証
   - ロールベースアクセス制御（RBAC）
   - `/api/submissions/*`: company role必須
   - `/api/admin/*`: reviewer/admin role必須

4. **UIログイン画面**:
   - Submission UI: 企業ユーザーログイン
   - Review UI: 管理者ログイン
   - セッション管理（localStorage + httpOnly cookie）

**成果物**:
- 認証サーバー
- JWT発行・検証ロジック
- ロールベースアクセス制御
- ログインUI

---

### Phase 5: 事業者登録機能（優先度: 中 🟡）

**目的**: 企業がアカウント登録し、複数エージェントを管理

**タスク**:
1. **組織DBスキーマ** (`db/migrations/20251114_organizations.sql`)
   ```sql
   CREATE TABLE organizations (
     id UUID PRIMARY KEY,
     name TEXT NOT NULL,
     contact_email TEXT NOT NULL,
     website TEXT,
     verified BOOLEAN DEFAULT false,
     created_at TIMESTAMP WITH TIME ZONE DEFAULT now()
   );

   ALTER TABLE submissions ADD COLUMN organization_id UUID REFERENCES organizations(id);
   ```

2. **組織登録UI** (`submission-ui/src/app/register-org/page.tsx`)
   - 組織情報入力フォーム
   - メール検証
   - 初期アカウント作成

3. **組織ダッシュボード** (`submission-ui/src/app/dashboard/page.tsx`)
   - 登録エージェント一覧
   - 各エージェントの信頼性スコア表示
   - 新規エージェント登録ボタン

**成果物**:
- 組織管理スキーマ
- 組織登録フロー
- 組織ダッシュボード

---

### Phase 6: LLM Judge Panel マルチモデルアンサンブル（優先度: 中 🟡）

**目的**: バイアス軽減と評価精度向上のため、複数LLMモデルで評価

**詳細設計**: `docs/design/llm-judge-panel-ensemble-design.md` を参照

**タスク**:
1. **マルチモデル設定** (`inspect-worker/inspect_worker/multi_model_judge.py`)
   - OpenAI (GPT-4o)
   - Anthropic (Claude 3.5 Sonnet)
   - Google (Gemini 2.0 Flash)

2. **Position Bias対策**:
   - プロンプト順序ランダム化
   - 各モデルで2回評価し平均化

3. **アンサンブル戦略実装**:
   - Minority-Veto: 30%以上が"reject"なら最終判定もreject
   - Weighted Voting: モデルの重みと信頼度を考慮

4. **Judge Orchestratorの置き換え**:
   - 既存のMCTS Judgeを新しいMulti-Model Judge Ensembleに置き換え

**成果物**:
- Multi-Model Judge実装
- Position Bias対策
- Minority-Veto戦略
- コスト分析レポート

---

### Phase 7: 信頼性スコアの自動更新（優先度: 低 🟢）

**目的**: 実運用で問題が発生したエージェントのスコアを自動的に下げる

**タスク**:
1. **インシデント報告API** (`api/src/routes/incidents.ts`)
   - POST `/api/agents/{agentId}/incidents`
   - インシデントタイプ（セキュリティ、機能不良、応答遅延など）
   - 重要度（critical, high, medium, low）

2. **スコア減算ロジック** (`api/src/services/trustScoreService.ts`)
   ```typescript
   const penaltyMap = {
     security: { critical: -30, high: -20, medium: -10 },
     functional: { critical: -40, high: -30, medium: -15 },
     implementation: { critical: -10, high: -7, medium: -5 }
   };
   ```

3. **再評価トリガー**:
   - スコアが一定値以下になったら自動的に再審査ワークフローを起動
   - 修正後の再提出を受け付け

4. **スコア履歴表示**:
   - エージェントダッシュボードにスコア変動グラフ
   - インシデント履歴と紐付け

**成果物**:
- インシデント報告API
- スコア減算ロジック
- 再評価トリガー
- スコア履歴可視化

---

## 📊 実装スケジュール（推奨）

| Phase | タスク | 優先度 | 想定工数 | 依存関係 |
|-------|--------|--------|----------|----------|
| 1.5 | 自動判定分岐ロジック | 🔴 最優先 | 1日 | Phase 1完了 |
| 2 | Review UIスコアカード | 🟠 高 | 2日 | Phase 1.5完了 |
| 3 | Submission UI | 🟠 高 | 3日 | Phase 1.5完了 |
| 4 | JWT認証 | 🟡 中 | 3日 | Phase 3完了 |
| 5 | 事業者登録 | 🟡 中 | 2日 | Phase 4完了 |
| 6 | Multi-Model Judge | 🟡 中 | 5日 | Phase 2完了 |
| 7 | スコア自動更新 | 🟢 低 | 3日 | Phase 5完了 |

**合計想定工数**: 約19日（約4週間）

---

## 🧪 テスト戦略

### 各Phaseで実施すべきテスト

#### Phase 1.5: 自動判定分岐
- [x] スコア<40のSubmissionが自動リジェクトされる
- [x] スコア≥80のSubmissionが自動承認される
- [x] スコア40-79のSubmissionが人間レビューに回される
- [x] DBに正しくスコアが永続化される
- [x] trust_score_historyに履歴が記録される

#### Phase 2: スコアカードUI
- [x] スコア内訳が正しく表示される
- [x] 判定根拠が読みやすく表示される
- [x] 自動判定結果が色分けされる

#### Phase 3: Submission UI
- [x] 無効なURLでバリデーションエラーが出る
- [x] エージェントカードが取得できないURLで警告が出る
- [x] 登録後にステータスページへリダイレクトされる
- [x] ステータスページでリアルタイム進行状況が見える

#### Phase 6: Multi-Model Judge
- [x] 3モデル全てが正常に評価を返す
- [x] Position Randomizationで評価のばらつきが減少する
- [x] Minority-Vetoで1モデルでもrejectなら最終判定もreject
- [x] コストが想定内（1質問あたり$0.0116以下）

---

## 📈 成功指標（KPI）

1. **自動化率**: 自動承認+自動リジェクトの割合（目標: 70%以上）
2. **人間レビュー精度**: 人間レビューが覆した判定の割合（目標: 10%以下）
3. **False Positive率**: 承認後に問題が発覚したエージェントの割合（目標: 5%以下）
4. **False Negative率**: リジェクト後に再評価で承認されたエージェントの割合（許容: 20%以下）
5. **平均審査時間**: PreCheckから最終判定までの時間（目標: 10分以内）

---

## 🔗 関連ドキュメント

- [LLM Judge Panel アンサンブル設計](./llm-judge-panel-ensemble-design.md)
- [信頼性スコアDBマイグレーション](../../db/migrations/20251114_trust_scores.sql)
- [Review Pipeline Workflow](../../prototype/temporal-review-workflow/src/workflows/reviewPipeline.workflow.ts)
- [プラットフォーム概要（README）](../../README.md#-プラットフォームの概要とミッション)
