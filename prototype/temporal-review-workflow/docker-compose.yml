version: '3.9'
services:
  temporal:
    image: temporalio/auto-setup:1.24.0
    environment:
      - DB=sqlite
    ports:
      - "7233:7233"
    healthcheck:
      test: ["CMD", "tctl", "cluster", "health"]
      interval: 5s
      timeout: 5s
      retries: 20
